spring:
  datasource:
    url: ${MYSQL_CONNECT}
    username: ${USERNAME}
    password: ${PASSWORD}
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
server:
  port: ${PORT}